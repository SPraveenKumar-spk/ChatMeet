realm=chatmeet.onrender.com  # Replace with Render's subdomain (or your custom domain)
external-ip=0.0.0.0         # Render automatically handles public IP assignments
use-auth-secret
static-auth-secret=anonymouschatmeet
fingerprint
total-quota=100             # User connection limit
bps-capacity=0              # Unlimited bandwidth
stale-nonce=600
listening-port=3478         # Port Render should expose
tls-listening-port=5349     # Optional TLS port
